@page "/my-frequent-services-table"
@using Microsoft.AspNetCore.Components.Web
@using Microsoft.AspNetCore.Components.Forms
@inject IJSRuntime JSRuntime
@using BlazorAzureServices.Services
@inject ConfigurationService ConfigurationService

<PageTitle>My Frequent Azure Services - Table View</PageTitle>

<div class="container">
    <div class="single-line-header">
        <a href="azure-services" class="back-btn">← Back To All Services</a>
        <a href="my-frequent-services" class="back-btn">📋 List View</a>
        <h1 class="inline-title">My Frequent Azure Services - Table View</h1>
        <input type="text" @bind="searchQuery" @oninput="OnSearchInput" placeholder="Search" class="search-input" />
    </div>

        @if (isLoading)
        {
            <div class="loading">Loading services...</div>
        }
        else if (!string.IsNullOrEmpty(errorMessage))
        {
            <div class="error">@errorMessage</div>
        }
        else
        {
            @foreach (var serviceSection in filteredServiceSections)
            {
                <div class="service-section">
                    <div class="section-header-with-toggle">
                        <button class="section-toggle-btn @(serviceSection.IsExpanded ? "expanded" : "collapsed")" 
                                @onclick="() => ToggleSection(serviceSection.Type)">
                            <span class="section-icon">@serviceSection.Icon</span>
                            @serviceSection.Name
                            <span class="toggle-icon">@(serviceSection.IsExpanded ? "▼" : "▶")</span>
                        </button>
                    </div>
                    
                @if (serviceSection.IsExpanded)
                {
                <div class="table-container-compact">
                    <table class="services-table-compact">
                        <thead>
                            <tr>
                                @foreach (var header in serviceSection.TableHeaders)
                                {
                                    <th>@header</th>
                                }
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var resource in serviceSection.Resources)
                            {
                                <tr @onclick="() => NavigateToResource(resource.Url)" class="compact-row">
                                    <td>
                                        <div class="table-service-name-compact">
                                            <span class="table-service-icon-compact">@resource.Icon</span>
                                            <span class="service-name-text">@resource.Name</span>
                                            @if (!string.IsNullOrEmpty(resource.Tag))
                                            {
                                                <span class="table-tag-compact @resource.Tag.ToLower()">@resource.Tag</span>
                                            }
                                        </div>
                                    </td>
                                    <td class="compact-cell">@resource.Type</td>
                                    <td class="compact-cell">@resource.ResourceGroup</td>
                                    <td class="compact-cell">@resource.Location</td>
                                    <td class="compact-cell">@resource.TierOrRuntime</td>
                                    @if (serviceSection.ShowExtraColumn)
                                    {
                                        <td class="compact-cell">
                                            @if (serviceSection.Type == "apimanagement" && !string.IsNullOrEmpty(resource.ExtraColumn))
                                            {
                                                <button type="button" class="table-btn-compact dev-portal" @onclick="() => NavigateToDevPortal(resource)" @onclick:stopPropagation="true">Portal</button>
                                            }
                                            else
                                            {
                                                @resource.ExtraColumn
                                            }
                                        </td>
                                    }
                                    <td class="compact-cell"><span class="table-status-compact @resource.StatusClass">@resource.Status</span></td>
                                    <td class="compact-cell">
                                        <button type="button" class="table-btn-compact view" @onclick="() => NavigateToResource(resource.Url)" @onclick:stopPropagation="true">View</button>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
                }
                </div>
            }
        }
</div>



@code {
    private List<ServiceTableSection> serviceSections = new();
    private List<ServiceTableSection> filteredServiceSections = new();
    private bool isLoading = true;
    private string errorMessage = "";
    private string searchQuery = string.Empty;

    protected override async Task OnInitializedAsync()
    {
        await LoadServiceSections();
    }

    private async Task LoadServiceSections()
    {
        try
        {
            isLoading = true;
            serviceSections = await ConfigurationService.LoadServiceSectionsAsync();
            // Expand all sections by default
            foreach (var section in serviceSections)
            {
                section.IsExpanded = true;
            }
            filteredServiceSections = serviceSections;
        }
        catch (Exception ex)
        {
            errorMessage = $"Error loading services: {ex.Message}";
            Console.WriteLine(errorMessage);
        }
        finally
        {
            isLoading = false;
            StateHasChanged();
        }
    }

    private void ToggleSection(string sectionType)
    {
        var section = serviceSections.FirstOrDefault(s => s.Type == sectionType);
        if (section != null)
        {
            section.IsExpanded = !section.IsExpanded;
            StateHasChanged();
        }
    }

    private void OnSearchInput(ChangeEventArgs e)
    {
        searchQuery = e.Value?.ToString() ?? string.Empty;
        FilterServices();
    }

    private void FilterServices()
    {
        if (string.IsNullOrEmpty(searchQuery))
        {
            filteredServiceSections = serviceSections;
        }
        else
        {
            filteredServiceSections = serviceSections.Select(section => new ServiceTableSection
            {
                Name = section.Name,
                Icon = section.Icon,
                Type = section.Type,
                IsExpanded = section.IsExpanded,
                TableHeaders = section.TableHeaders,
                Resources = section.Resources.Where(resource =>
                    resource.Name.Contains(searchQuery, StringComparison.OrdinalIgnoreCase) ||
                    resource.Type.Contains(searchQuery, StringComparison.OrdinalIgnoreCase) ||
                    resource.ResourceGroup.Contains(searchQuery, StringComparison.OrdinalIgnoreCase) ||
                    resource.Location.Contains(searchQuery, StringComparison.OrdinalIgnoreCase) ||
                    (!string.IsNullOrEmpty(resource.TierOrRuntime) && resource.TierOrRuntime.Contains(searchQuery, StringComparison.OrdinalIgnoreCase))
                ).ToList()
            }).Where(section => section.Resources.Any()).ToList();
        }
        StateHasChanged();
    }

    private async Task NavigateToResource(string? url)
    {
        if (!string.IsNullOrEmpty(url))
        {
            await JSRuntime.InvokeVoidAsync("window.open", url, "_blank");
        }
    }

    private async Task NavigateToDevPortal(TableResourceItem resource)
    {
        // For API Management services, construct the developer portal URL
        if (!string.IsNullOrEmpty(resource.Name))
        {
            // Typical Azure API Management developer portal URL pattern
            var devPortalUrl = $"https://{resource.Name}.developer.azure-api.net";
            await JSRuntime.InvokeVoidAsync("window.open", devPortalUrl, "_blank");
        }
    }
}