<div class="nav-scrollable">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All" title="Home">
                <span class="nav-icon">🏠</span>
                @if (!IsCollapsed)
                {
                    <span class="nav-text">Home</span>
                }
            </NavLink>
        </div>
        
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="azure-services" title="Azure Services">
                <span class="nav-icon">☁️</span>
                @if (!IsCollapsed)
                {
                    <span class="nav-text">Azure Services</span>
                }
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="my-frequent-services" title="My Frequent Services">
                <span class="nav-icon">⭐</span>
                @if (!IsCollapsed)
                {
                    <span class="nav-text">My Frequent Services</span>
                }
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="my-frequent-services-table" title="Table View">
                <span class="nav-icon">📊</span>
                @if (!IsCollapsed)
                {
                    <span class="nav-text">Table View</span>
                }
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="bitbucket-branching-diagram" title="Branching Diagram">
                <span class="nav-icon">🌳</span>
                @if (!IsCollapsed)
                {
                    <span class="nav-text">Branching Diagram</span>
                }
            </NavLink>
        </div>

        <hr />

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="counter" title="Counter">
                <span class="nav-icon">🔢</span>
                @if (!IsCollapsed)
                {
                    <span class="nav-text">Counter</span>
                }
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="weather" title="Weather">
                <span class="nav-icon">🌤️</span>
                @if (!IsCollapsed)
                {
                    <span class="nav-text">Weather</span>
                }
            </NavLink>
        </div>
    </nav>
</div>

@code {
    [Parameter] public bool IsCollapsed { get; set; } = false;
}

